[project]
name = "agent-zero-dday"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pendulum>=3.1.0",
    "psycopg-binary>=3.2.6",
]

[tool.uv.workspace]
members = [
    "agent_zero",
    "backend_server",
]
